#To enable SSCP mode from Command Line Interface (CLI)* type command 'sscp'

--Send any message listed in SSCP-Messages.txt

--To enable CLI mode send message frame "Switch to CLI mode"





*CLI using Hyperterminal / Teraterm 